<div class="modal-dialog modal-dialog-centered modal-lg">
  <div class="modal-content">

    <div class="modal-header">
      <h3>Edit Wish</h3>
    </div>

    <div class="modal-body">
      <%= form_for :list_item, url: update_list_item_path(list_item.id), method: :patch do |f| %>
        <%= render partial: 'form_contents', locals: { f: f, list_item: list_item } %>
      <% end %>
    </div>
  </div>
</div>

<style>

.success {
  position: absolute;
  right: 0;
  top: -40px;
  background: #9cdba6;
  color: green;
  font-weight: 600;
  border-radius: 5px;
  padding: .25rem 2rem;
  animation: successful 0.4s forwards;
}

@keyframes successful {
  from {
    top: -40px;
  }
  to {
    top: -60px;
  }
}

textarea {
  height: 100px;
}

.cancel {
  border: 2px solid #c62e2e;
  color: #c62e2e;
  font-weight: 600;
}

.submit {
  background-color: #9cdba6;
    color: green;
    font-weight: 600;
  border-radius: 10px;
}

.modal form label {
  font-size: 1rem;
}

.modal-content {
  border: none;
  border-radius: 15px;
}

.modal-body {
  padding: 1rem 2rem 2rem 2rem;
}

.modal-header {
  padding: 2rem 2rem 1rem 2rem;
  border: none;
  display: flex;
  justify-content: center;
}

</style>